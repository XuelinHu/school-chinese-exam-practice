node_modules/
dist/
logs/
.tmp/
.playwright-mcp/

.DS_Store
*.log
*.tmp

admin/logs/
fronter/dist/

# 真实密码只留在本地 .env，示例文件 .env.example 可以提交
.env
.env.local
.env.*.local
!.env.example

# 用户上传（头像等）不入库
admin/uploads/

# 软著材料成品（设计说明书 / 用户手册 / 源码稿，含真实界面截图与整份源码提取）。
# 这是对外提交用的交付件，不进版本库、更不进公开远端：源码稿是整份代码的逐字提取，
# 手册里是真实运行界面。交付走 softright/ 目录本身，不靠 git 分发。
# 与 .tmp/ 分开写：.tmp/ 是中间产物，这里是成品，混淆了两边都会漏。
softright/
